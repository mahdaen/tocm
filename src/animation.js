// CREATING TOCM ANIMATION LIBRARY.
(function (window) {
    'use strict';
    require(['tocm']);

    var animation = function (name, duration, easing) {

    };

    window.$animation = window.TocmAnimation = animation;
})(window);